import React, { useEffect, useState } from 'react';
function Ville(props) {
    return <border>
        <center>{props.city}</center>
        <center>{props.weather}</center>
        <center>{props.heat}</center>
    </border>
}
export default Ville;
